以下是针对这四个功能的测试提示词，每个场景都包含具体的使用说明和评估标准。

---

## 6.1 评估

### 测试 1：递归查找目录树中的所有 `.c` 文件

**提示词：**
```
请递归搜索当前目录下的所有 .c 文件，列出它们的完整路径，并统计总共有多少个文件。对于每个找到的文件，显示它的文件大小（以字节为单位）。最后按文件大小从大到小排序输出结果。
```

**评估指标：** 成功率、工具调用轮数、令牌消耗

---

### 测试 2：探索不熟悉的代码库并回答问题

**提示词：**
```
你刚刚接手了一个位于/home/sjc/桌面/arch/project2-handout/project2-handout/project2-handout的开源项目，你之前从未见过这个代码库。请完成以下任务：

1. 首先，浏览项目的顶层目录结构，识别这是什么类型的项目（Web应用、库、CLI工具等）
2. 找到主要的入口文件（如 main.c、index.js、app.py 等）
3. 阅读入口文件，总结项目的主要功能
4. 找到项目的依赖配置文件，列出所有外部依赖
5. 回答：这个项目中有没有定义错误处理或日志记录的机制？如果有，在哪里？

请以结构化的方式呈现你的发现。
```

**评估指标：** 答案准确性、工具调用轮数、探索深度

---

### 测试 3：精确的多文件文本替换

**提示词：**
```
在 /home/user/project 目录中，有一个包含多个 Python 文件的代码库。现在需要做一个安全升级：

1. 找到所有使用了 `pickle.load()` 的文件（可能有多个）
2. 在每个文件中，将 `pickle.load(file)` 替换为 `pickle.load(file, fix_imports=True, encoding="utf-8")`
3. 但是，不要修改任何注释中的代码、文档字符串中的示例、或者已经在 `try-except` 块中使用了 `fix_imports` 参数的调用
4. 替换完成后，列出所有被修改的文件和具体的修改位置（行号）

注意：不要破坏任何无关代码，确保替换后的代码语法仍然正确。
```

**评估指标：** 替换精确度（无遗漏、无误伤）、语法正确性保持、令牌消耗

---

### 测试 4：跨文件重构任务

**提示词：**
```
在 /home/user/web-app 项目中，有一个名为 `UserModel` 的类定义在 src/models/user.py 中。现在需要将这个类重命名为 `AccountModel`。

请完成以下操作：
1. 找到所有引用了 `UserModel` 的文件（包括导入语句、类型注解、实例化等）
2. 将所有的 `UserModel` 引用替换为 `AccountModel`
3. 将定义文件 user.py 重命名为 account.py
4. 更新所有相关的导入路径
5. 验证：替换后没有遗留任何 `UserModel` 的引用

注意区分大小写，不要修改 `user_model`（小写下划线）这样的变量名。
```

**评估指标：** 重构完整性、路径更新正确性、遗漏检测

---

## 6.2 子代理

### 测试 1：并行代码审查

**提示词：**
```
请对当前项目进行一次全面的代码审查。这个审查包含三个独立的维度：

1. 代码质量检查：查找所有超过 100 行的函数、重复代码块、以及可能的性能瓶颈
2. 安全检查：查找 SQL 注入、XSS 漏洞、硬编码的密钥或不安全的加密算法
3. 文档完整性检查：查找所有没有文档字符串的公共函数和类

请为每个维度创建一个子代理来独立处理，主代理负责汇总三个子代理的结果，生成一份统一的审查报告，并按严重程度（严重/警告/建议）对发现的问题进行分类。
```

**评估指标：** 子代理并行效率、主上下文令牌节省量、结果汇总质量

---

### 测试 2：多仓库依赖分析

**提示词：**
```
你需要分析一个微服务架构项目的依赖关系。这个项目有三个独立的服务仓库：
- /home/user/services/api-gateway
- /home/user/services/user-service  
- /home/user/services/order-service

请为每个服务仓库创建一个子代理，分别完成：
1. 分析该服务使用了哪些第三方库（从 package.json / requirements.txt 等文件中提取）
2. 找出该服务中定义的 API 端点或 RPC 接口
3. 识别该服务调用了哪些其他服务的接口

所有子代理完成后，主代理汇总结果，生成一张服务依赖关系图，并标记出循环依赖（如果存在）。
```

**评估指标：** 子代理独立运行能力、依赖图准确性、上下文精简效果

---

### 测试 3：混合任务分流

**提示词：**
```
在 /home/user/fullstack-app 项目中，你需要同时处理三件事：
1. 检查前端代码（frontend/）是否符合 ESLint 规范
2. 运行后端代码（backend/）的单元测试，并报告测试结果
3. 扫描整个项目的依赖文件，找出存在已知漏洞的依赖包

请使用子代理分别处理这三个任务。前端任务只需要关注 frontend/ 目录，后端任务只需要关注 backend/ 目录，安全扫描需要扫描整个项目。主代理等待所有子代理完成后，生成一份综合状态报告。
```

**评估指标：** 任务隔离程度、主上下文污染程度、报告整合质量

---

## 6.3 会话

### 测试 1：中断后恢复

**提示词：**
```
这是一个两步任务。首先请完成第一步：

第一步：在 /home/user/project 中，找出所有包含 TODO 注释的文件，记录每个 TODO 的内容和所在位置（文件名、行号），将结果保存到 /tmp/todo_list.json 中。

（此时模拟会话中断：关闭终端或退出程序）

—— 会话恢复后 ——

第二步：基于你之前保存的 /tmp/todo_list.json 文件，从第一个 TODO 开始，逐一分析每个 TODO 的复杂度和优先级。对于标记为"高优先级"的 TODO，尝试自动实现它；对于"中优先级"的，给出实现建议；对于"低优先级"的，直接跳过。请确保你能回忆起第一步的结果。
```

**评估指标：** 恢复后状态完整性、是否重复工作、上下文连贯性

---

### 测试 2：跨会话项目开发

**提示词：**
```
会话 A 的任务：
在 /home/user/new-project 中初始化一个新的 Python 项目。设置项目结构（src/、tests/、docs/），创建 setup.py 或 pyproject.toml，编写一个基础的 CLI 入口函数。记录你的所有设计决策到 /tmp/design_decisions.md。

（会话 A 结束）

—— 新会话 B 开始 ——

恢复之前的会话后，基于你在 design_decisions.md 中记录的设计决策，为这个项目的 CLI 入口添加一个新的子命令 `version`，它能打印项目的版本号。请先回顾之前做了什么，再继续开发。
```

**评估指标：** 历史回放准确性、设计决策遵守度、无缝衔接程度

---

### 测试 3：崩溃恢复与数据完整性

**提示词：**
```
正在执行一个关键任务：将 /home/user/data 目录中的 100 个 CSV 文件合并成一个大的数据集，进行清洗（去重、填充缺失值），然后保存到 /home/user/output/merged_clean.csv。

在处理到第 45 个文件时，系统突然崩溃。

—— 恢复后 ——

请从崩溃点恢复任务。你应该能够：
1. 识别哪些文件已经处理过了
2. 从第 45 个文件继续处理，而不是重新开始
3. 确保最终合并的数据集中没有重复数据（即第 1-44 个文件的数据不会重复加入）
```

**评估指标：** 断点续传能力、数据完整性、幂等性保证

---

## 6.4 记忆

### 测试 1：项目约定学习与持久化

**提示词：**
```
会话 A（学习阶段）：
请探索 /home/user/project 项目，学习以下信息并保存到项目记忆中（.agent/memory/）：
1. 代码风格：缩进方式、命名规范、注释风格
2. 项目结构约定：模块组织方式、测试文件放置规则
3. 构建系统：使用的构建工具、常用构建命令
4. Git 分支策略：当前使用的分支命名规范
5. 在代码中找到的所有特殊约定或非标准做法

（会话 A 结束）

—— 新会话 B 开始 ——

现在请在项目中创建一个新的功能模块：一个名为 `UserValidator` 的类，放在合适的位置。编写时请严格遵循你在之前的会话中学到的所有项目约定。不需要再重新探索代码风格——直接从记忆中加载。
```

**评估指标：** 记忆加载速度、约定遵守准确度、无需重复探索

---

### 测试 2：跨会话决策记忆

**提示词：**
```
会话 A：
在讨论 /home/user/project 的重构方案时，我们做了以下决定：
1. 将旧的 XML 配置文件迁移到 YAML 格式
2. 数据库查询层从 ORM 改为原生 SQL（出于性能考虑）
3. 测试框架从 unittest 迁移到 pytest
4. 决定保留旧版 API 两个版本周期，标记为 deprecated

请将这些决策记录到项目记忆中。

（会话 A 结束）

—— 会话 B 开始（两周后）——

现在要对项目进行新的改动：添加一个新的 REST API 端点用于用户数据导出。请在实现时：
1. 遵循我们之前关于配置文件格式的决定（使用 YAML）
2. 使用我们之前选择的数据库查询方式（原生 SQL）
3. 使用正确的测试框架编写测试（pytest）
4. 不要修改任何已标记为 deprecated 的旧 API 代码

从记忆中加载相关决策，确保一致性。
```

**评估指标：** 决策回忆准确性、一致性遵守、记忆检索效率

---

### 测试 3：记忆更新与冲突处理

**提示词：**
```
会话 A：
学习并记忆以下关于 /home/user/project 的信息：
- API 端点：GET /api/users（获取用户列表）
- API 端点：POST /api/users（创建用户）
- 认证方式：JWT Token
- 数据库：PostgreSQL，连接池大小 20
保存到 .agent/memory/project_info.json

（会话 A 结束）

—— 会话 B 开始 ——

项目中发生了一些变化：
- 新增了 DELETE /api/users/{id} 端点
- JWT Token 现在增加了 refresh token 机制
- 数据库连接池大小调整为 50

请更新项目记忆：
1. 添加新的 API 端点信息
2. 更新认证机制的描述
3. 更新数据库配置参数
4. 如果发现记忆中的信息与当前代码不一致（比如连接池还是 20），请标记冲突并询问如何处理
```

**评估指标：** 增量更新准确性、冲突检测能力、记忆一致性维护

---

这些测试提示词涵盖了四个功能的核心场景，每个都可以独立运行并产出可量化的评估结果。





2
请对当前项目进行一次全面的代码审查。这个审查包含三个独立的维度：

1. 代码质量检查：查找所有超过 100 行的函数、重复代码块、以及可能的性能瓶颈
2. 安全检查：查找 SQL 注入、XSS 漏洞、硬编码的密钥或不安全的加密算法
3. 文档完整性检查：查找所有没有文档字符串的公共函数和类

请为每个维度创建一个子代理来独立处理，主代理负责汇总三个子代理的结果，生成一份统一的审查报告，并按严重程度（严重/警告/建议）对发现的问题进行分类。